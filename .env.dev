# =============================================================================
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# WARNING: These are insecure default values for LOCAL DEVELOPMENT ONLY!
# NEVER use these values in production or any publicly accessible environment.
# =============================================================================

# App Configuration
BASE_URL=http://localhost:8080
PORT=8080
ENVIRONMENT=development

# Admin Authentication (DEVELOPMENT ONLY - Change for production!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=dev_only_insecure

# Database Configuration (DEVELOPMENT ONLY - Change for production!)
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_DB=id100
DATABASE_URL=postgres://dev:dev@db:5432/id100

# S3/MinIO Configuration (S3-compatible object storage)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=id100-images
S3_BUCKET_NAME=id100-images
S3_REGION=us-east-1
S3_ENDPOINT=http://minio:9000  # Internal Docker endpoint for S3 API calls
S3_PUBLIC_URL=http://localhost:9000  # Browser-accessible URL for image serving
MINIO_SERVER_URL=http://localhost:9000
MINIO_BROWSER_REDIRECT_URL=http://localhost:9001

# Session Security (DEVELOPMENT ONLY - Generate random secret for production!)
SESSION_SECRET=dev_session_secret_insecure_local_only

# Meilisearch Configuration
MEILI_NO_ANALYTICS=true
MEILI_MASTER_KEY=masterKey
MEILI_SEARCH_KEY=masterKey

# Geocoding API Configuration (Meilisearch with GeoNames data)
GEOCODING_API_URL=http://localhost:8081