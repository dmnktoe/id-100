{
  "name": "id-100",
  "version": "1.0.0",
  "description": "Innenstadt ID 100 - Client-side TypeScript modules",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --sourcemap --outfile=web/static/main.js",
    "build:dev": "esbuild src/main.ts --bundle --sourcemap --outfile=web/static/main.js",
    "watch": "esbuild src/main.ts --bundle --sourcemap --watch --outfile=web/static/main.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@sentry/browser": "^8.47.0",
    "meilisearch": "^0.44.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "@vitest/ui": "^4.0.18",
    "esbuild": "^0.27.2",
    "happy-dom": "^20.5.0",
    "jsdom": "^28.0.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  }
}
